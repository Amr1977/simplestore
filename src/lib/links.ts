/**
 * External links surfaced across the storefront.
 * Update the values here when the portfolio or APK release channel is finalized.
 */

export const PORTFOLIO_URL = 'https://example.com/portfolio'

/**
 * Latest APK release page. This is the link surfaced from the /download page
 * and the in-app update prompt. It is the GitHub Releases "latest" URL.
 *
 * To make this work end-to-end, set up a release workflow that uploads the
 * signed APK to the repo's Releases page on every tag push.
 */
export const APK_RELEASES_URL = 'https://github.com/abu-qir-grocery/abu-qir-grocery/releases/latest'

/**
 * Direct download URL for the latest APK asset, when known.
 * Leave as the empty string to let the UI redirect to APK_RELEASES_URL instead.
 */
export const APK_LATEST_URL = ''

export const STORE_NAME = 'بقالة أبو قير'
